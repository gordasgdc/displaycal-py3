"""Standalone DisplayCAL tools ported to Qt.

Each module here exposes a ``main()`` entry point and a top-level window built
on :mod:`DisplayCAL.ui`. They double as the reference implementations for the
wx-to-Qt migration pattern (see ``DisplayCAL/ui/README.md``).
"""
